classdef EditRegions
    
    % Mesh edit map identifiers.  These are used via EditRegions.MATERIAL etc.
    % The user can add more if needed.  Ideas could be to denote CMFD regions
    % (if an automated approach isn't taken).  
    enumeration 
        ASSEMBLY, PIN 
    end
    
end